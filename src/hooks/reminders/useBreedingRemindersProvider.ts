
// Re-export useReminderProvider with the original name for backward compatibility
import { useReminderProvider } from './useReminderProvider';

export const useBreedingRemindersProvider = useReminderProvider;
